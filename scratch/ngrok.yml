version: "2"
# Replace YOUR_AUTH_TOKEN with your actual ngrok auth token
# Get it from: https://dashboard.ngrok.com/get-started/your-authtoken
authtoken: 3634fakHLfnsr0XltEylSKggf1l_56Gp7EadV6k5puJwxm1AH
#authtoken: YOUR_AUTH_TOKEN

tunnels:
  # Tunnel for Vite Dev Server (Frontend)
  vite:
    proto: http
    addr: 5173
    host_header: "localhost:5173"
    # Uncomment below to use a reserved domain (requires paid plan)
    domain: unsplendid-ofelia-ponderously.ngrok-free.dev
  
  # Tunnel for PeerJS Server (WebRTC Signaling)
  peerjs:
    proto: http
    addr: 9000
    # Uncomment below to use a reserved domain (requires paid plan)
    # domain: your-peerjs-domain.ngrok-free.app
